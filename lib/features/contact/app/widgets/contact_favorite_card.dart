import 'package:flutter/cupertino.dart';

class ContactFavoriteCard extends StatelessWidget {
  const ContactFavoriteCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
